# !path is a hint to Python that the following string is a path.
overlay_path: !path "overlay"
sim_config: !path "configs/latest"
sim_img: !path "root.img"
sim_prog: !path "kernel.bin"
log_dir: !path "logs"
partitions: [ "firesim", ]
nodelist: [ "colbyjack", ]
